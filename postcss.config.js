module.exports = {
  // Define the list of PostCSS plugins.
  plugins: {
    // Use the Tailwind CSS plugin to process and optimize CSS.
    tailwindcss: {},

    // Use Autoprefixer to add vendor prefixes for improved browser compatibility.
    autoprefixer: {},
  },
};
